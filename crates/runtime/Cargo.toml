[package]
name = "minicontainer-runtime"
version = "1.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libc = "0.2"
minicontainer-bundle = { path = "../bundle" }
minicontainer-protocol = { path = "../protocol" }
minios-abi.workspace = true
